import 'package:flutter/material.dart';

class FormPendataanPage extends StatelessWidget {
  const FormPendataanPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Form Pendataan'),
      ),
      body: const Center(
        child: Text('Form Pendataan Page'),
      ),
    );
  }
}

