import 'package:flutter/material.dart';

class DetailLampuPage extends StatelessWidget {
  const DetailLampuPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Detail Lampu'),
      ),
      body: const Center(
        child: Text('Detail Lampu Page'),
      ),
    );
  }
}

